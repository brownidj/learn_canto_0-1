[pytest]
addopts = -m "not ui"
markers =
    ui: tests that require a Qt GUI environment
    pure: tests that are pure (no Qt / no external side effects)
    slow: tests that are slow or integration-heavy